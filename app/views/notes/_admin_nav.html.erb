<% if logged_in? && action_name != "new" %>
  <section class="admin">
    <ul>
      <li class="edit">  <%= link_to_unless_current "Edit",    edit_note_path(note) %></li>
      <li class="delete"><%= link_to "Destroy", note, method: :delete, data: { confirm: "Are you sure?" } %></li>
    </ul>
  </section>
<% end %>
